get:
  summary: Get hello world message
  tags:
    - Hello
  responses:
    '200':
      description: Successful response
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Message'
